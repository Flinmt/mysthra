{
  "name": "mysthra",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Self-hosted collaborative worldbuilding platform backend",
  "main": "src/server.js",
  "scripts": {
    "dev": "node --env-file=.env --watch src/server.js",
    "start": "node --env-file=.env src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@hocuspocus/server": "^4.0.0",
    "ws": "^8.20.0",
    "yjs": "^13.6.30"
  }
}
