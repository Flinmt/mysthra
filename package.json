{
  "name": "mysthra",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "description": "Self-hosted collaborative worldbuilding platform backend",
  "main": "src/server.js",
  "scripts": {
    "dev": "npm --prefix client run build && node --env-file=.env --watch src/server.js",
    "dev:api": "node --env-file=.env --watch src/server.js",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "start": "node --env-file=.env src/server.js",
    "test": "node --test --test-concurrency=1 test/*/*.test.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@hocuspocus/server": "^4.0.0",
    "ws": "^8.20.0",
    "yjs": "^13.6.30"
  }
}
